# Copy to .env and adjust. All keys use the APP_ prefix.
APP_APP_NAME=cat-only-svg-api
APP_ENVIRONMENT=development
APP_DEBUG=false
APP_HOST=127.0.0.1
APP_PORT=8000
# JSON list of allowed origins
APP_CORS_ORIGINS=["*"]
