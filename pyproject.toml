[project]
name = "system-api"
version = "0.1.0"
description = "A real-life RPG System: quests, EXP, levels, stats, and penalties"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "pydantic-settings>=2.4",
    "sqlalchemy>=2.0",
    "pyjwt>=2.8",
    "email-validator>=2.0",
    "sqlalchemy-cloudflare-d1>=0.3.11",
]

[project.optional-dependencies]
local = [
    "uvicorn[standard]>=0.30",
    "alembic>=1.13",
]
dev = [
    "pytest>=8.0",
    "httpx>=0.27",
]

[dependency-groups]
worker = [
    "workers-py",
    "workers-runtime-sdk",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*", "scripts*"]
